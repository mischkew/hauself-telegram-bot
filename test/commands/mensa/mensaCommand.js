import { expect } from 'chai'
import debug from 'debug'
import moment from 'moment'
import { today,
         tomorrow,
         nextMonday,
         isWeekend,
         hours,
         nextMealDate,
         menuToMarkdown
       } from '../../../src/commands/mensa/mensaCommand'
const log = debug('test')

describe('Mensa Command', () => {
  // TODO
})
